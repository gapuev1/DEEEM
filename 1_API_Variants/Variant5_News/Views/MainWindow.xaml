<Window x:Class="YourApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📰 Новости" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate x:Key="NewsCardTemplate">
            <Border Background="White" CornerRadius="8" Margin="5" Padding="10" BorderBrush="#DDD" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding ImageUrl}" Width="80" Height="80" Stretch="UniformToFill" Margin="0,0,10,0"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding DisplayText}" FontSize="11" Foreground="Gray" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Description}" FontSize="10" Foreground="#666" TextWrapping="Wrap" MaxHeight="40"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#E74C3C" Padding="20">
            <TextBlock Text="📰 Новости" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="White" Padding="15" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox" Grid.Column="0" FontSize="14" Padding="10" Margin="0,0,10,0" ToolTip="Введите тему"/>
                <Button x:Name="SearchBtn" Grid.Column="1" Content="🔍 ПОИСК" Background="#E74C3C" Foreground="White" FontSize="14" FontWeight="Bold" Padding="20,10" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <ListBox x:Name="ResultsList" Grid.Row="2" Margin="10" ItemTemplate="{StaticResource NewsCardTemplate}"/>

        <Border Grid.Row="3" Background="#E74C3C" Padding="10">
            <TextBlock x:Name="StatusText" Text="Введите тему для поиска новостей" Foreground="White" FontSize="12"/>
        </Border>
    </Grid>
</Window>