<Window x:Class="YourApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📚 Поиск книг" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate x:Key="BookCardTemplate">
            <Border Background="White" CornerRadius="8" Margin="5" Padding="10" BorderBrush="#DDD" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="{Binding CoverUrl}" Width="60" Height="80" Stretch="UniformToFill" Margin="0,0,10,0"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding Author}" FontSize="11" Foreground="Gray"/>
                        <TextBlock Text="{Binding Year}" FontSize="10" Foreground="#666"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="20">
            <TextBlock Text="📚 Поиск книг" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="White" Padding="15" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox" Grid.Column="0" FontSize="14" Padding="10" Margin="0,0,10,0" ToolTip="Введите название книги"/>
                <Button x:Name="SearchBtn" Grid.Column="1" Content="🔍 ПОИСК" Background="#3498DB" Foreground="White" FontSize="14" FontWeight="Bold" Padding="20,10" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <ListBox x:Name="ResultsList" Grid.Row="2" Margin="10" ItemTemplate="{StaticResource BookCardTemplate}"/>

        <Border Grid.Row="3" Background="#2C3E50" Padding="10">
            <TextBlock x:Name="StatusText" Text="Введите название книги" Foreground="#BDC3C7" FontSize="12"/>
        </Border>
    </Grid>
</Window>